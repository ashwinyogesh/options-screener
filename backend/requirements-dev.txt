# Backend dev dependencies (tests, lint, type-check).
# Install with:
#   .\venv\Scripts\python.exe -m pip install -r requirements-dev.txt
pytest>=8.0
pytest-cov>=5.0
pytest-socket>=0.7
freezegun>=1.5
ruff>=0.15
pre-commit>=4.0
